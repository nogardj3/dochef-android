<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.yhjoo.dochef.ui.viewmodel.RecipeListViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/recipetheme_toolbar"
            style="@style/BaseTheme.Toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:theme="@style/BaseTheme.Toolbar"
            app:layout_constraintTop_toTopOf="parent"
            app:title="추천" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recipetheme_recycler"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/recipetheme_toolbar"
            tools:listitem="@layout/recipetheme_item" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>